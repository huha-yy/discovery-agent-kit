# Pre-Release Checklist

Minimum checks before release or customer walkthrough:

- application starts successfully
- expected port and runtime mode are confirmed
- database path and migrations are correct
- scheduler and worker settings are understood
- key UI tabs load
- key APIs return expected responses
- key tests are executed or intentionally waived
- acceptance scope and excluded items are written down

If any item is waived, record why.
